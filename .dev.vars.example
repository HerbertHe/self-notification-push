# Deploy to Cloudflare reads this file as the template's secret declaration.
# Keep optional APNs, Basic Auth and FilterBox destination secrets out of this
# file; configure them after deployment only when needed.
BACKDOOR_API_KEY="replace-with-a-random-admin-key"
